<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="12dp"/> <!-- Скругление углов -->
    <solid android:color="@android:color/transparent"/> <!-- Прозрачный цвет для не проверенного состояния -->
    <stroke android:width="2dp" android:color="@android:color/white"/> <!-- Окантовка для не проверенного состояния -->
    <padding android:left="10dp" android:right="10dp" android:top="10dp" android:bottom="10dp"/> <!-- Отступы внутри -->
</shape>
