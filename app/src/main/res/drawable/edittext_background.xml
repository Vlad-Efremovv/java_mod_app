<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="8dp"/> <!-- Скругление углов -->
    <solid android:color="@android:color/white"/> <!-- Цвет фона -->
    <stroke android:width="1dp" android:color="@android:color/darker_gray"/> <!-- Окантовка -->
    <padding android:left="10dp" android:right="10dp" android:top="10dp" android:bottom="10dp"/> <!-- Отступы внутри -->
</shape>
