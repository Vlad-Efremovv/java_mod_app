<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <size
        android:width="24dp"
        android:height="24dp" /> <!-- Размер окружности -->
    <solid android:color="@android:color/transparent" /> <!-- Цвет не проверенного радио-кнопки -->
    <stroke
        android:width="2dp"
        android:color="@android:color/holo_red_light" /> <!-- Цвет окантовки не проверенного состояния -->
</shape>