<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <corners android:radius="16dp" />  <!-- Скругление углов -->
    <solid android:color="@android:color/background_light" />  <!-- Цвет фона -->
    <padding
        android:bottom="15dp"
        android:left="15dp"
        android:right="15dp"
        android:top="15dp" />  <!-- Отступы внутри спиннера -->
</shape>
